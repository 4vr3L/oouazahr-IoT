apiVersion: v1
kind: Service
metadata:
  name: iot-app
  namespace: dev
spec:
  selector:
    app: iot-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8087
      nodePort: 30000
  type: NodePort